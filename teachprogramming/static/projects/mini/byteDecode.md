https://mbrserver.com/FDO/
https://masterbootrecord.bandcamp.com/album/floppy-disk-overdrive

GCSE?

```python
tt = """
"""
print(bytes(map(int, filter(None, map(lambda i: i.strip().lstrip('0'), tt.split(" "))))).decode('utf8'))
```